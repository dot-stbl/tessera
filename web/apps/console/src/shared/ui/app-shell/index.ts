// Tessera app shell — topnav + sidebar + main layout.
// Generic primitives live in ../primitives. APM-specific live in ../apm.

export { AppShell } from './app-shell';
export type { AppShellProps } from './app-shell';

export { PageTemplate } from './page-template';
export type { PageTemplateProps } from './page-template';

export { APP_SECTIONS, APP_SECTION_BY_PATH } from './nav-config';
export type { AppSection } from './nav-config';