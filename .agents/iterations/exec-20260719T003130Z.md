---
generated: 2026-07-19T00:31:30.991Z
kind: exec
soly_dir: C:\Users\bradw\source\stbl\tessera\.agents
---

# Iteration Context — Execute Phase undefined / Plan undefined

**Generated**: 2026-07-19T00:31:30.991Z
**Soly dir**: `.agents`

> **Read this file first.** It contains all the context you need for this iteration.
> If you need the full unabridged version of any section, the source path is given in the section header.

## 0. Project Intent (from `.agents/docs/`)

_Source: `.agents/docs/` (full files in `.agents/docs/`, see also `soly_intent` tool)_

- `docs\architecture.md`: > APM UI for the Victoria stack. Trace viewer with log correlation, like Kibana APM. **Status:** pre-implementation. Design captured here; code scaffold follows once PLAN.md is approved. Self-hosted web app that proxies the Victoria telemet
- `docs\architecture\config-format.md`: > **Decision (locked 2026-07-19):** TOML only. No `appsettings.json` in source. Tessera loads configuration from TOML files. Strict schema, comments, XDG-style paths, no .NET-isms leaking into deploy artifacts. Secrets are in env vars, not 
- `docs\architecture\log-format.md`: > **Decision (locked 2026-07-19):** `dot.case` at export, PascalCase in source. Tessera uses OpenTelemetry log SDK with custom `DotCaseLogRecordProcessor` to enforce **dot.case** for all log attribute keys at export. Code may use PascalCase
- `docs\architecture\multi-tenancy.md`: > **Decision (locked 2026-07-19):** Single-tenant, config-time. Tessera MVP is single-tenant. The tenant ID is **hardcoded in `tessera.toml`**, not derived from request headers or user identity. One Tessera deployment = one tenant. - Self-h
- `docs\dashboard-schema.md`: > **Decision (locked 2026-07-19):** Custom JSON format, versioned, Grafana-inspired structure. Tessera dashboards are JSON files with a custom schema inspired by Grafana's layout. The format is **versioned** (`schemaVersion`) and **declarat
- `docs\ecosystem.md`: > **Tessera is a product of the `.stbl` studio** (GitHub org `github.com/dot-stbl`). > This doc captures the conventions that flow from that membership so agents > don't have to re-derive them every time. A small open-source studio (per `gi
- `docs\modules.md`: > Per-module contracts. Each module is a self-contained vertical slice in > `src/modules/Tessera.Modules.<Feature>/`. Modules don't reference each other > directly — communication via interfaces in `Tessera.Shared.*` or via > composition ro

_(truncated at 500 tokens; full files in `.agents/docs/`)_


## 1. Project State (`.agents/STATE.md` — Current Position + Decisions)

_Source: `.agents\STATE.md`_

## Decisions

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-07-19 | Tessera positioning: Grafana-analogue APM (multi-provider), NOT Victoria-specific UI | MVP-01 ships with Victoria as first provider via new `src/providers/` layer; future phases add Tempo/Jaeger/Loki/Mimir without refactoring modules. Architecture: provider interfaces in `Tessera.Shared.Kernel`, concrete implementations in new `src/providers/` top-level layer. No own collector/storage layer — reuse external collectors. |
| 2026-07-19 | New top-level `src/providers/` folder for concrete provider implementations | Mirrors Grafana datasource model: providers pluggable, modules provider-agnostic. Composition root wires active provider(s) in DI. |
| 2026-07-19 | Provider interfaces (`ITraceProvider`, `ILogProvider`, `IMetricsProvider`, `IDiscoveryProvider`, `IHealthProvider`) in `Tessera.Shared.Kernel` | Kernel already holds domain primitives; provider contracts are natural extension. Keeps `shared/` at 5-project cap. |
| 2026-07-19 | MVP-01 module scope: 4 modules — Traces + Logs + Discovery + Health | Matches HANDOFF baseline. Health as DI verification canary, Discovery aggregates services + streams. |
| 2026-07-19 | Victoria is MVP-01 first-and-only provider; second provider deferred to MVP-02+ | User explicitly chose "build abstraction now" over "defer to MVP-02". Cheap upfront, no refactor later. |

---

**End of iteration context.** Continue with the workflow instructions in the task prompt.